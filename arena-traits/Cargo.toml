[package]
name = "arena-traits"
version = "0.2.0-alpha.1"
edition = "2021"
license = "CC0-1.0"
description = "Traits for arenas"

[dependencies]
id-arena = { workspace = true, optional = true }

[features]
id-arena = ["dep:id-arena"]
